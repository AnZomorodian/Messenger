## Packages
framer-motion | Smooth animations for messages and page transitions
date-fns | Formatting message timestamps
lucide-react | Beautiful icons for the UI (already in base, but ensuring availability)

## Notes
- Polling strategy: Messages poll every 1s, Users poll every 5s
- Authentication: Client-side persistence using localStorage (simulated session)
- Design: Glassmorphism with vibrant gradients
